#  Revise `qa_summary.py` to add a column in group-wise to list missing subject

## Goal

Update `qa_summary.py` so it will add a column in group-wise to list missing subject.
All tables (from table 1 to table 7) need to add relevant column.

Use the latest project context file as the source of truth:

```text
PROJECT_CONTEXT.md
```

