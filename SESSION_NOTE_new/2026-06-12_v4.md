# Revise `qa_summary.py` to add subject as part of configure

Update `qa_summary.py` so it will add subject_id as part of configure. So that users can define subject groups to include or exclude in the summary. 

Use the latest project context file as the source of truth:

```text
PROJECT_CONTEXT.md
```

