# Revise `qa_summary.py` to add configure section

## Goal

Update `qa_summary.py` so it will add a configure section so user can configure expected sessions and instrument based on needed

Use the latest project context file as the source of truth:

```text
PROJECT_CONTEXT.md
```

